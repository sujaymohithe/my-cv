import { DetailSkeleton } from "@/components/ui";

/**
 * A component that displays a loading state for a detail view.
 * It renders a DetailSkeleton component.
 * @returns A JSX element representing the loading state.
 */
export default function Loading() {
  return <DetailSkeleton />;
}
